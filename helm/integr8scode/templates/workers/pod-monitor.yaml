{{- if .Values.workers.podMonitor.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "integr8scode.fullname" . }}-pod-monitor
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "integr8scode.labels" . | nindent 4 }}
    app.kubernetes.io/component: pod-monitor
spec:
  replicas: {{ .Values.workers.podMonitor.replicas | default 1 }}
  selector:
    matchLabels:
      app: {{ include "integr8scode.fullname" . }}-pod-monitor
  template:
    metadata:
      labels:
        app: {{ include "integr8scode.fullname" . }}-pod-monitor
        {{- include "integr8scode.labels" . | nindent 8 }}
        app.kubernetes.io/component: pod-monitor
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/secrets/env-secret.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "integr8scode.serviceAccountName" . }}
      containers:
      - name: pod-monitor
        image: {{ include "integr8scode.backendImage" . }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy | default "IfNotPresent" }}
        command:
          {{- toYaml .Values.workers.podMonitor.command | nindent 10 }}
        envFrom:
        - secretRef:
            name: {{ include "integr8scode.fullname" . }}-env
        env:
        - name: KAFKA_CONSUMER_GROUP_ID
          value: {{ .Values.workers.podMonitor.consumerGroupId | quote }}
        - name: TRACING_SERVICE_NAME
          value: "pod-monitor"
        - name: KUBECONFIG
          value: "/app/kubeconfig.yaml"
        resources:
          {{- if .Values.workers.podMonitor.resources }}
          {{- toYaml .Values.workers.podMonitor.resources | nindent 10 }}
          {{- else }}
          {{- toYaml .Values.workers.common.resources | nindent 10 }}
          {{- end }}
        volumeMounts:
        - name: kubeconfig
          mountPath: /app/kubeconfig.yaml
          subPath: kubeconfig.yaml
          readOnly: true
      volumes:
      - name: kubeconfig
        secret:
          secretName: {{ .Values.kubeconfig.secretName }}
      restartPolicy: Always
{{- end }}
