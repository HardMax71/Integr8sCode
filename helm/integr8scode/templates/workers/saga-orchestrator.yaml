{{- if .Values.workers.sagaOrchestrator.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "integr8scode.fullname" . }}-saga-orchestrator
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "integr8scode.labels" . | nindent 4 }}
    app.kubernetes.io/component: saga-orchestrator
spec:
  replicas: {{ .Values.workers.sagaOrchestrator.replicas | default 1 }}
  selector:
    matchLabels:
      app: {{ include "integr8scode.fullname" . }}-saga-orchestrator
  template:
    metadata:
      labels:
        app: {{ include "integr8scode.fullname" . }}-saga-orchestrator
        {{- include "integr8scode.labels" . | nindent 8 }}
        app.kubernetes.io/component: saga-orchestrator
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmaps/env-configmap.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "integr8scode.serviceAccountName" . }}
      containers:
      - name: saga-orchestrator
        image: {{ include "integr8scode.backendImage" . }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy | default "IfNotPresent" }}
        command:
          {{- toYaml .Values.workers.sagaOrchestrator.command | nindent 10 }}
        envFrom:
        - configMapRef:
            name: {{ include "integr8scode.fullname" . }}-env
        env:
        - name: KAFKA_CONSUMER_GROUP_ID
          value: {{ .Values.workers.sagaOrchestrator.consumerGroupId | quote }}
        - name: TRACING_SERVICE_NAME
          value: "saga-orchestrator"
        resources:
          {{- if .Values.workers.sagaOrchestrator.resources }}
          {{- toYaml .Values.workers.sagaOrchestrator.resources | nindent 10 }}
          {{- else }}
          {{- toYaml .Values.workers.common.resources | nindent 10 }}
          {{- end }}
      restartPolicy: Always
{{- end }}
